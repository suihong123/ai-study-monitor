# 项目协作约定

本仓库以 `docs/` 下的项目基线作为产品、业务和技术沟通的共同上下文。

## 优化前必须完成的复盘

任何产品、交互、业务逻辑、数据结构、AI 策略、运营后台、部署或架构优化，在讨论方案或修改代码前都必须：

1. 阅读 `docs/PROJECT_BASELINE.md`、`docs/BUSINESS_STATUS.md` 和 `docs/ITERATION_PLAYBOOK.md`。
2. 检查当前工作区、最近提交和本次涉及的代码，确认基线是否仍然准确。
3. 使用 `docs/reviews/TEMPLATE.md` 新建或更新一份优化前复盘，文件名为 `YYYY-MM-DD-主题.md`。
4. 先向项目负责人说明：
   - 当前阶段和已确认事实；
   - 本次问题及业务影响；
   - 目标、范围与不做事项；
   - 验收标准、风险和待确认项。
5. 获得共同上下文后，再进入具体方案和实施。

如果只是回答问题或做只读排查，可以不新建复盘文件，但仍应先核对项目基线。紧急故障可先止损，随后在同一轮工作中补齐复盘记录。

## 文档维护

- 代码、产品规则或业务口径发生变化时，同一轮更新相应基线文档。
- “已确认事实”“判断/假设”“未知/待验证”必须分开记录。
- 不把 Mock 能力、规划能力或营销表述写成已上线事实。
- 不凭空填写用户数、收入、转化率、留存率和线上成本。
- 每次复盘完成后，将结论追加到 `docs/reviews/README.md` 索引。

## 权益与设备规则

- 访问码永久有效，只按总监督分钟消耗；不得重新引入自然日/月/季/年到期。
- 一个访问码任意时刻只绑定一台设备；自助换绑不能扩展成多设备同时使用。
- 设备换绑的免费次数、冷却、时长扣减、幂等和日志必须由服务端数据库事务控制，客户端只提交换绑意图和一次性请求 ID。
- 修改设备换绑时必须保留第一轮会话原子结算，不把换绑扣时长写入单次监督时长或报告。
- 涉及数据库字段或函数时同时更新 `supabase/schema.sql` 和增量迁移，并按 Migration → Deploy → 验收顺序上线。

## 当前权威入口

- 项目全景：`docs/PROJECT_BASELINE.md`
- 业务现状：`docs/BUSINESS_STATUS.md`
- 迭代机制：`docs/ITERATION_PLAYBOOK.md`
- 复盘记录：`docs/reviews/`
